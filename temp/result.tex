\documentclass[12pt]{article}
\pagestyle{empty}
\thispagestyle{empty}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{fullpage}
\usepackage{enumerate}
\usepackage[mathscr]{euscript}
\usepackage{graphicx}
\setlength{\parskip}{0.1in}

\newlength{\toppush}
\setlength{\toppush}{2\headheight}
\setlength{\parindent}{0.0in}
\addtolength{\toppush}{\headsep}

\newtheorem{conjecture}{Conjecture}
\newtheorem{proposition}{Proposition}
\newtheorem{theorem}{Theorem}
\newtheorem{claim}{Claim}
\newtheorem{definition}{Definition}
\newtheorem{note}{Note}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newenvironment{proof}{{\em Proof:}}{\hfill\rule{2mm}{2mm}}


\def\subjnum{COMP 170}
\def\subjname{Theory of Computation}
\def\doheading#1#2#3{\vfill\eject\vspace*{-\toppush}%
  \vbox{\hbox to\textwidth{{\bf} \subjnum: \subjname \hfil Prof. Lenore  Cowen} %
    \hbox to\textwidth{{\bf} Tufts University \hfil#3\strut}%
    \hrule}}

\newcommand{\htitle}[1]{\vspace*{3.25ex plus 1ex minus .2ex}%
  \begin{center}{\large\bf #1}\end{center}}


\begin{document}

\doheading{2}{title}{Spring 2021}
\htitle{HW 2: due Thursday, February 25 at 9pm}

\medskip
\subsubsection*{Problem 1}

%For any string $w = w_1\ldots w_n$, the {\em reverse of $w$,\/} written $w_{R}$, is the string in reverse order, $w_n \ldots w_2w_1$. For any language $A$, let $A^{R} = \{ w_{R} \
%| w \in A \}$. Show that if $A$ is regular, so is $A^{R}$.
a) Prove that the following language on binary strings is regular:
$$   D =  \{    \mbox{ All binary strings that either start with two 0's or end with three 1's (but not both)  }\}                                      $$
\smallskip
b) Prove that the following language over the English 26 letter alphabet  is regular: 
$$ R = \{ w \mid w \mbox{ contains an even number of occurrences of the  substring "go" }$$ 
$$ \mbox{ and the last go is part of the   substring "gone" } \} $$

\newpage

\subsubsection*{Problem 2}

Do these two regular expressions represent the same language?

$$ aa(a \cup b)^* \cup (bb)^*a \mbox{ and } (ab \cup ba \cup a)^* $$

\newpage


\subsubsection*{Problem 3}

Prove that the following language is not regular:
$ \{a^mb^nc^pd^q: m+n = p+q \}$

\newpage

\subsubsection*{Problem 4}

Is the language of Problem 3 context free?

\newpage

\subsubsection*{Problem 5}

For any string $w = w_1\ldots w_n$, the {\em reverse of $w$,\/} written $w_{R}$, is the string in reverse order, $w_n \ldots w_2w_1$. For any language $A$, let $A^{R} = \{ w_{R} \
| w \in A \}$. Show if $A$ is regular, so is $A^{R}$.


%Prove or disprove:
%\begin{enumerate}
%\item the context free languages are closed under union
%\item the context free languages are closed under intersection
%\item the context free languages are closed under complement
%\end{enumerate}




%\end{enumerate}



\end{document}


